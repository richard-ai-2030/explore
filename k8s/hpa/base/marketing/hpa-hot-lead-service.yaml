apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: hot-lead-service
  namespace: explore
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: hot-lead-service
  minReplicas: 2
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
