apiVersion: apps/v1
kind: Deployment
metadata:
  name: quotation-service
  namespace: explore
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quotation-service
  template:
    metadata:
      labels:
        app: quotation-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /metrics
        prometheus.io/port: "7112"
    spec:
      containers:
        - name: quotation-service
          image: quotation-service:version3
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 7112
          env:
            - name: REDIS_URL
              value: "redis://172.19.0.6:6379/0"
            - name: REDIS_PREFIX
              value: "quotation-service"
            - name: DEFAULT_CACHE_TTL_SECONDS
              value: "600"
            - name: PORT
              value: "7112"
            - name: POSTGRES_HOST
              value: "172.19.0.1"
            - name: POSTGRES_PORT
              value: "5432"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "postgres"
            - name: POSTGRES_DB
              value: "quotation_service"
            - name: KAFKA_ENABLED
              value: "true"
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: "172.19.0.1:9092"
            - name: KAFKA_EVENTS_TOPIC
              value: "explore.events"
---
apiVersion: v1
kind: Service
metadata:
  name: quotation-service
  namespace: explore
spec:
  selector:
    app: quotation-service
  ports:
    - name: http
      port: 7112
      targetPort: 7112
